#pragma once

#include <vector>

namespace pc {
namespace algo {
    
/// calculate in how many ways we can make a change with given coins
int coin_exchange(const std::vector<int>& coins, int n);

}
}

